export const regexEmailPattern =(/[^\s@]+@[^\s@]+\.[^\s@]+/);
export const accessTokenLifetime = 1000 * 60 *15; // it is 15 min
export const refreshTokinLifeTime = 1000 * 60 * 60* 24 *30 ; // 24 hours
